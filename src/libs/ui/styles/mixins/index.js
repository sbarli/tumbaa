import respond from './respond';
import { borderImage } from './borders';
import { removeMargin, removePadding } from './elementSpacing';

const mixins = {
  respond,
  borderImage,
  removeMargin,
  removePadding,
};

export default mixins;